dataset_path: "${hydra:runtime.cwd}/src/$RVNS6MQ" 
sample_rate: 16000
duration: 3.0 # seconds limitations -> time 
n_mels: 128 # fix -> attention & n_mels relation 
n_fft: 1024 # Fix
hop_length: 512
f_min: 0.0
f_max: 8000.0 # Nyquist frequency at 16k is 8k
# pad_mode: "constant" # reason & meaning
normalize: true # reason & meaning
